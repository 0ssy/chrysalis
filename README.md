# Chrysalis

A GPU-native engine for resolving dependency-ordered workloads in parallel
using speculative execution and conflict detection, validated across
seven domains (blockchain execution, causal log verification, SCC
decomposition, build graphs, merge conflict detection, static timing
analysis, sparse Cholesky/ILU).

## Status
Early scaffolding. Core CSR DAG + resolution strategies in progress.
